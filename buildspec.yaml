version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    runtime-versions:
      python: 3.7
      # name: version
    commands:
      - apt-get update -y
      
      # - command
  pre_build:
    commands:
      - pip install -r requirements.txt
      # - command
  build:
    commands:
      # - command
      # - command
  post_build:
    commands:
      - echo Build completed on `date`
#artifacts:
  #files:
    # - location
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths